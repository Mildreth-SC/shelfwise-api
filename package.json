{
  "name": "shelfwise-api",
  "version": "2.3.0",
  "description": "ShelfWise API — préstamos + AuthService (login, bcrypt, 2FA) — ejemplo académico",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "lint": "node --check src/PrestamoService.js && node --check src/CatalogoService.js && node --check src/AuthService.js && node --check src/SmsService.js && node --check src/index.js",
    "test": "node --test tests/",
    "test:all": "npm run lint && npm test",
    "migrate:passwords": "node scripts/migrarPasswords.js"
  },
  "keywords": ["github", "best-practices", "security", "2fa", "shelfwise"],
  "author": "Mildreth-SC",
  "license": "MIT",
  "engines": { "node": ">=18" },
  "dependencies": {
    "bcrypt": "^5.1.1"
  }
}


