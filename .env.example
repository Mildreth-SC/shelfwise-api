# Copia este archivo a .env y completa los valores.
# NUNCA subas el archivo .env real al repositorio.

PORT=3000
NODE_ENV=development

# Tokens de ejemplo (ficticios) — en producción usar GitHub Secrets
SHELFWISE_API_KEY=replace_me
DB_URL=postgres://user:pass@localhost:5432/shelfwise
